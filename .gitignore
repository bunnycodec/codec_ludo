# Secrets — never commit real credentials
.env

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/

# Local dev database
*.db

# macOS Finder metadata
.DS_Store

# Local-only holding area (e.g. source images for the About page) — never committed
Archive/

# Node (frontend, later phases)
node_modules/
dist/
